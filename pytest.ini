[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    network: touche le réseau réel. Exclu par défaut, se lance à la main :
        ./scripts/test.sh -m network
addopts = -m "not network"
