"""Le ticket de caisse : le lire, le valider, le rapprocher.

`parse.py` est pur. `task.py` est le SEUL module de ce paquet qui sorte du
processus — comme `off/client.py` est le seul à parler à Open Food Facts.
"""
