pytest-homeassistant-custom-component==0.13.356

# These two are NOT test tools. custom_components/home_stock/receipt/task.py
# imports homeassistant.components.ai_task, whose entity module imports
# homeassistant.components.conversation, which imports both at module level.
# A real Home Assistant install has them, because conversation declares them
# as its own requirements; the wheel above installs Home Assistant WITHOUT the
# per-integration requirements, so two test modules fail at COLLECTION with
# ModuleNotFoundError - which nothing had ever noticed, because ruff failed
# before pytest was ever reached on this repository.
#
# The versions are the ones homeassistant/components/conversation/manifest.json
# declares in 2026.8.2, the release the pin above resolves to. Bumping
# pytest-homeassistant-custom-component can move them: read that manifest in
# the Home Assistant release it brings in.
hassil==3.11.0
home-assistant-intents==2026.7.30

# ruff is pinned HERE, and this is the only place that can pin it. The shared
# workflow (nivuus/.github ci-python.yml) ends its dependency step with a bare
# `pip install ruff pytest`, which resolves to whatever was published that
# morning; but it installs THIS file first, so a version already satisfied is
# the version the control runs with. Unpinned, the rule set moves under the
# repository between releases - see ruff.toml for the measurement and for the
# selection this code is written against.
ruff==0.16.6
